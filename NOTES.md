# cr-sqlite

What to figure out:
- gossip format
- sync format
- backup + restore
- soft deletes
- filters + subscriptions (dynamic exprt schema)
- schema versioning (hash of sqlite_master for each table?)